# Legacy backup / orphaned DB dump — kept on disk per CLAUDE.md safety
# rules, but not versioned as source (large, not part of the rebuild).
byteandbook-backup-2026-08-26.zip
bytesbra_wp928.sql

# Astro/Node build tooling (Phase 4+)
site/node_modules/
site/dist/
site/.astro/
site/preview*.log
.env
.env.*

# V2.0.1 — content-hashed CSS build artifacts (regenerated by
# `npm run build`/`npm run dev`, see scripts/hash-css.mjs). The raw
# Tailwind CLI output, the hashed public/ copy, and the generated
# manifest module BaseLayout imports are all build output, not source.
site/.tmp/
site/public/styles.css
site/public/styles.*.css
site/src/generated/

# OS/editor cruft
.DS_Store
Thumbs.db
